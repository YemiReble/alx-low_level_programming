#include <stdio.h>
#include <ctype.h>

/**
* main - Entry point
*
* Return: Always 0 (Success)
*
*/

int main(void)
{
	int m;

	for(m ='z';m<='a'; m--)
	{
		putchar(m);
	}
	putchar('\n');

	return (0);
}
